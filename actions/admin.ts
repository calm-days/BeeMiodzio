"use server";

export async function publishUpdate(_formData: FormData) {
  // TODO: Create update entry with photos
  return { success: true };
}

export async function assignHive(_formData: FormData) {
  // TODO: Assign hive to subscription
  return { success: true };
}
